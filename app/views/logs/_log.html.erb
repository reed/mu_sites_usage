<tr>
  <td><%= log.device_info_toggler %></td>
  <td><%= log.client.site.display_name if log.client.site.present? %></td>
  <td><%= log.vm %></td>
  <td><%= log.user %></td>
  <td><%= format_log_time(log.login_time) if log.login_time.present? %></td>
  <td><%= format_log_time(log.logout_time) if log.logout_time.present? %></td>
  <td><%= log.duration %></td>
</tr>