<%= form_for(@site) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Create" %>
    <%= link_to "Cancel", sites_path, :class => "button" %>
  </div>
<% end %>

<%= render 'shared/name_filter_explanation' %>
