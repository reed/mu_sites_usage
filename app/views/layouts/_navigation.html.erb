<nav id="main_nav">
  <h1><a href="<%= url_for(root_path) %>"><span>Sites Usage</span></a><h1>
  <h3><%= image_tag "sublogo_dark.jpg", :title => "Division of IT" %></h3>
  <ul class="menu">
    <%= department_links %>
  </ul>
  <ul id="submenu">
    <% if can? :read, Log %>
      <li <%= "class=active" if submenu_link_active? "logs" %>><%= link_to "Logs", logs_path %></li>
    <% end %>
    <% if can? :read, StatsController %>
      <li <%= "class=active" if submenu_link_active? "stats" %>><%= link_to "Statistics", stats_path %></li>
    <% end %>
    <% if can? :read, User %>
      <li <%= "class=active" if submenu_link_active? "users" %>><%= link_to "Users", users_path %></li>
    <% end %>
    <% if can? :read, Client %>
      <li <%= "class=active" if submenu_link_active? "clients" %>><%= link_to "Device Management", clients_path %></li>
    <% end %>
    <% if can? :manage, Site %>
      <li <%= "class=active" if submenu_link_active? "sites" %>><%= link_to "Site Management", sites_path %></li>
    <% end %>
    <% if signed_in? %>
      <li><%= link_to "Log Out", logout_path, :method => :delete %></li>
    <% else %>
      <li <%= "class=active" if submenu_link_active? "sessions" %>><%= link_to "Log In", login_path %></li>
    <% end %>
  </ul>
  <div id="copy">&copy; 2012 University of Missouri<br />Division of Information Technology</div>
</nav>
  